[tools]
pnpm = "10"

[tasks."wiki:install"]
description = "Install wiki dependencies"
run = "pnpm install"
dir = "docs"

[tasks."wiki:dev"]
description = "Start wiki dev server"
depends = ["wiki:install"]
run = "pnpm dev"
dir = "docs"

[tasks."wiki:build"]
description = "Build wiki for production"
depends = ["wiki:install"]
run = "pnpm build"
dir = "docs"
