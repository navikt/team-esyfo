info:
  name: hent transmission
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/serviceowner/dialogs/{{dialogId}}/transmissions/{{transmissionId}}"
  auth: inherit

runtime:
  variables:
    - name: transmissionId
      value: 019e011c-564c-71be-8605-339ef4618907

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
